# Atlas Phase 1 — core runtime
python-dotenv>=1.0.0
PyYAML>=6.0

# Slack remote control (Phase 3)
slack-sdk>=3.27.0
slack-bolt>=1.18.0
aiohttp>=3.9.0

# Memory
# sqlite3 is stdlib

# Browser verification (Phase 4)
playwright>=1.40.0

# Git safety (Phase 6)
GitPython>=3.1.0
# langgraph>=0.2.0
# anthropic>=0.25.0
